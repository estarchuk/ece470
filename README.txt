Prerequisites
 - Python

How to Run

pip install pygad
pip install pandas

python3 ./genetic_algorithm.py

If it the first run make sure the first_sample variable is set to true, then it will provide randomized samples.

You will need to provide growth rates and current sizes of samples, when run with first_sample=False. It will create and read from files in the same directory. 